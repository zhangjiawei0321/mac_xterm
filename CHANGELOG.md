# 更新日志

## [1.0.2] - 2025-09-02

### ✨ 新增
- **开始保存接下来的日志**（SecureCRT 风格）：从点击那一刻起，把后续输出实时追加写入所选文件（写入前自动剥离 ANSI 颜色码/控制序列，文件为干净文本，与屏幕显示一致），可随时停止；标签页与窗口标题栏用红点提示记录中。入口：右键终端空白处，或标签页右键菜单。
- 顶部 **帮助 → 版本信息…**：可查看每个版本优化了什么（快捷键 ⌥⌘I）。

### 🔧 调整
- 「保存日志」改名为「**保存当前日志…**」，与「保存接下来的日志」区分开。
- **日志缓存上限只作用于「保存之前的日志」**：设置里的上限不再影响屏幕显示（显示不限量），也不再限制「保存接下来的日志」（实时记录完整写入文件，与上限无关）；正在录制时不再弹出上限询问，避免中断记录过程。

## [1.0.1] - 2025-09-01

### ✨ 新增
- **会话日志缓存上限**：日志改为环形缓存，设置里可调上限（默认 10MB，0 为不限）。超过上限后较早内容自动丢弃，并弹窗询问一次性处理方式：**保存全部 / 只保存最近一部分 / 继续丢弃**，避免长时间会话日志无限占用内存。

### 🐛 修复
- **修复终端输入数字"每满 4 个字符才落屏"**：根因是 IP 着色器为保护"可能还没写完的 IP 地址"，会把输出流尾部的数字/点扣在缓冲区（最多扣 3 位）；shell 逐字回显时，只有凑够第 4 个字符才整体放行。现已改为所有字节即时上屏，IP 着色只对完整出现在同一块输出中的地址生效。
- **修复数字输入框难输入 / 显示卡顿**：缓存上限、端口、波特率、行间延迟这几个数字框，早前（format:.number）会吞掉输入中间状态、逐键写模型导致卡顿；现改用原生 NSTextField，输入即时显示、只在回车/失焦/保存时落盘。
- **调浅可执行文件的绿色**：亮绿色由纯 (0,255,0) 调为更柔和浅的绿，减轻长时间看 `ls` 输出的晃眼感（终端 16 色调色板）。

### 🧩 其他
- 数字输入框（缓存上限/端口/波特率/行间延迟）改走原生 NSTextField（NSViewRepresentable），规避 macOS 14 上 SwiftUI TextField 的输入显示回归。
- 版本号由 1.0.0 提升至 1.0.1（BundleID 不变：com.nblityterm.app，数据自动沿用）。
